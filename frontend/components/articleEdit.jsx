var React = require('react');
var PropTypes = React.PropTypes;
var ArticleNew = ('./articleNew');

var ArticleEdit = React.createClass({

  render: function() {
    debugger
    return (
      <ArticleNew />
    );
  }

});

module.exports = ArticleEdit;
