var React = require('react');
var PropTypes = React.PropTypes;

var LogIn = React.createClass({

  render: function() {
    return (
      <form>

      </form>
    );
  }

});

module.exports = LogIn;
